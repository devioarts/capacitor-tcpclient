{
  "name": "capacitor-app",
  "version": "1.0.0",
  "description": "An Amazing Capacitor App",
  "type": "module",
  "keywords": [
    "capacitor",
    "mobile"
  ],
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "sync": "npx cap sync",
    "build AND sync ANDROID": "vite build && npx cap sync android",
    "build AND sync IOS": "vite build && npx cap sync ios",
    "build AND sync ALL": "vite build && npx cap sync",
    "open-ios": "npx cap open ios",
    "open-android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "latest",
    "@devioarts/capacitor-tcpclient": "file:..",
    "@capacitor/ios": "^7.4.3",
    "@capacitor/android": "7.4.3"
  },
  "devDependencies": {
    "@capacitor/cli": "latest",
    "vite": "^7.1.3"
  },
  "author": "",
  "license": "ISC"
}